<div class="full-page">
  <div class="center-it inner-page">
    <h1 class="page-title">Notes</h1>
    <%= link_to 'Add New Note', new_note_path, class: "button add-new-link" %>
  <table>
    <thead>
      <% @notes.each do |type, note| %>
        <% unless @notes[type].empty? %>
          <tr>
            <th class="header", scope="col"><%= type %></th>
            <th></th>
          </tr>
        <% end %>
      </thead>
      <tbody>
        <% note.each do |info| %>
          <tr>
            <td class="additional-info"><%= sanitize(info.additional_info) %></td>
            <td><%= link_to 'Edit', edit_note_path(info), class: "button edit-note-link" %></td>
          </tr>
        <% end %>
      <% end %>
      </tbody>
  </table>
  </div>
</div>
